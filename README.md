# Firestore backups

Daily snapshots of `players/*` from Firestore (player doc + `exercises` + `coach_sessions` subcollections). Generated by `.github/workflows/backup.yml`. One file per player per day at `backups/YYYY-MM-DD/{player}.json`.
